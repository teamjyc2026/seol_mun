/**
 * 지문·문제 텍스트의 시각 서식 마크업 규칙 — 문제 생성·OCR 추출 프롬프트가
 * 공유하는 화이트리스트 태그. RichText 렌더러(src/shared/ui/RichText)와 일치.
 */
export const MARKUP_RULES = `[시각 서식 마크업 — 빈칸·밑줄 등은 아래 태그를 그대로 써라. 불필요하면 쓰지 마라. 화이트리스트 외 태그·실제 HTML 금지, 일반 부등호 <,>는 그대로 둬도 됨]
- 밑줄: <u>단어</u>, 번호 달린 밑줄(어법/어휘 선택지)은 <u n="1">word</u>
- 네모/박스(어휘 택1): <box>네모</box>
- 원문자(문장 삽입 위치): <num>③</num>
- 문단(글의 순서 (A)(B)(C)): <p label="A">문단</p>
- 빈칸(빈칸추론): <blank/>
- 굵게: <b>강조</b>, 기울임(italic): <i>강조</i>
- 표/도표: 마크다운 표(| 머리 | … | 와 |---|---| 구분줄)로 그대로 옮겨라.
- 사진·그림·다이어그램처럼 글로 못 옮기는 시각자료는 본문에 넣지 말고 [그림] 자리표시만 남겨라(이미지는 따로 첨부).`;
